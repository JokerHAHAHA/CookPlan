{% extends "base.html.twig" %}


{% block body %}

    {{parent()}}

    <div class="row">
        <div class="col s12">
            <ul class="tabs">
                <li class="tab col s3"><a href="#aperitifs">Apéritifs</a></li>
                <li class="tab col s3"><a href="#starters">Entrées</a></li>
                <li class="tab col s3"><a href="#meals">Viandes & Poissons</a></li>
                <li class="tab col s3"><a href="#garnishes">Légumes</a></li>
                <li class="tab col s3"><a href="#desserts">Dessert</a></li>
                <li class="tab col s3"><a href="#drinks">Boissons</a></li>
            </ul>
        </div>


        <div id="aperitifs" class="col s12">
            <a href="addMeal.php?cat=UN APERITIF" class="waves-effect waves-light center-align btn col s4 offset-s4 btn-add">ENREGISTRER UN APERITIF</a>
            
            <div class="col s8 offset-s2" >
                {% for meal in meals %}
                    {% if meal['category']=='aperitif' %}
                        <div class="divider"></div>
                        <h3 class="inline-block">{{meal['name']}}</h3>
                        <form action="meals.php?target={{meal['id']}}" class="form-delete right" method="post">
                            <button type="submit" name="btn-delete" class="btn-delete waves-effect waves-light btn red"><i class="large material-icons">delete</i></button>
                        </form>
                        <form action="updateMeal.php?target={{meal['id']}}" class="form-update right" method="post">
                            <button type="submit" name="btn-update" class="btn-update waves-effect waves-light btn orange"><i class="large material-icons">mode_edit</i></button>
                        </form>
                    {% endif %}
                {% endfor %}
            </div>
            
        </div>

        <div id="starters" class="col s12">
            <a href="addMeal.php?cat=UNE ENTREE" class="waves-effect waves-light center-align btn col s4 offset-s4 btn-add">ENREGISTRER UNE ENTREE</a>

            <div class="col s8 offset-s2" >
                {% for meal in meals %}
                    {% if meal['category']=='starter' %}
                        <div class="divider"></div>
                        <h3 class="inline-block">{{meal['name']}}</h3>
                        <form action="meals.php?target={{meal['id']}}" class="form-delete right" method="post">
                            <button type="submit" name="btn-delete" class="btn-delete waves-effect waves-light btn red"><i class="large material-icons">delete</i></button>
                        </form>
                        <form action="updateMeal.php?target={{meal['id']}}" class="form-update right" method="post">
                            <button type="submit" name="btn-update" class="btn-update waves-effect waves-light btn orange"><i class="large material-icons">mode_edit</i></button>
                        </form>
                    {% endif %}
                {% endfor %}
            </div>

        </div>
        <div id="meals" class="col s12">
            <a href="addMeal.php?cat=UN PLAT" class="waves-effect waves-light center-align btn col s4 offset-s4 btn-add">ENREGISTRER UNE VIANDE OU POISSON</a>

            <div class="col s8 offset-s2" >
                {% for meal in meals %}
                    {% if meal['category']=='meal' %}
                        <div class="divider"></div>
                        <h3 class="inline-block">{{meal['name']}}</h3>
                        <form action="meals.php?target={{meal['id']}}" class="form-delete right" method="post">
                            <button type="submit" name="btn-delete" class="btn-delete waves-effect waves-light btn red"><i class="large material-icons">delete</i></button>
                        </form>
                        <form action="updateMeal.php?target={{meal['id']}}" class="form-update right" method="post">
                            <button type="submit" name="btn-update" class="btn-update waves-effect waves-light btn orange"><i class="large material-icons">mode_edit</i></button>
                        </form>
                    {% endif %}
                {% endfor %}
            </div>

        </div>
        <div id="garnishes" class="col s12">
            <a href="addMeal.php?cat=UN ACCOMPAGNEMENT" class="waves-effect waves-light center-align btn col s4 offset-s4 btn-add">ENREGISTRER UN LEGUME</a>

            <div class="col s8 offset-s2" >
                {% for meal in meals %}
                    {% if meal['category']=='garnish' %}
                        <div class="divider"></div>
                        <h3 class="inline-block">{{meal['name']}}</h3>
                        <form action="meals.php?target={{meal['id']}}" class="form-delete right" method="post">
                            <button type="submit" name="btn-delete" class="btn-delete waves-effect waves-light btn red"><i class="large material-icons">delete</i></button>
                        </form>
                        <form action="updateMeal.php?target={{meal['id']}}" class="form-update right" method="post">
                            <button type="submit" name="btn-update" class="btn-update waves-effect waves-light btn orange"><i class="large material-icons">mode_edit</i></button>
                        </form>
                    {% endif %}
                {% endfor %}
            </div>

        </div>
        <div id="desserts" class="col s12">
            <a href="addMeal.php?cat=UN DESSERT" class="waves-effect waves-light center-align btn col s4 offset-s4 btn-add">ENREGISTRER UN DESSERT</a>

            <div class="col s8 offset-s2" >
                {% for meal in meals %}
                    {% if meal['category']=='dessert' %}
                        <div class="divider"></div>

                        <h3 class="inline-block">{{meal['name']}}</h3>
                        <form action="meals.php?target={{meal['id']}}" class="form-delete right" method="post">
                            <button type="submit" name="btn-delete" class="btn-delete waves-effect waves-light btn red"><i class="large material-icons">delete</i></button>
                        </form>
                        <form action="updateMeal.php?target={{meal['id']}}" class="form-update right" method="post">
                            <button type="submit" name="btn-update" class="btn-update waves-effect waves-light btn orange"><i class="large material-icons">mode_edit</i></button>
                        </form>
                    {% endif %}
                {% endfor %}
            </div>

        </div>
        <div id="drinks" class="col s12">
            <a href="addMeal.php?cat=UNE BOISSON" class="waves-effect waves-light center-align btn col s4 offset-s4 btn-add">ENREGISTRER UNE BOISSON</a>

            <div class="col s8 offset-s2" >
                {% for meal in meals %}
                    {% if meal['category']=='drink' %}
                        <div class="divider"></div>
                        <h3 class="inline-block">{{meal['name']}}</h3>
                        <form action="meals.php?target={{meal['id']}}" class="form-delete right" method="post">
                            <button type="submit" name="btn-delete" class="btn-delete waves-effect waves-light btn red"><i class="large material-icons">delete</i></button>
                        </form>
                        <form action="updateMeal.php?target={{meal['id']}}" class="form-update right" method="post">
                            <button type="submit" name="btn-update" class="btn-update waves-effect waves-light btn orange"><i class="large material-icons">mode_edit</i></button>
                        </form>
                    {% endif %}
                {% endfor %}
            </div>

        </div>
    </div>


{% endblock %}
